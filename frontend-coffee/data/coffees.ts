// Deprecated: this hardcoded data is no longer used.
// Products are now fetched from the backend API (GET /api/products).
export const coffeeProducts = [];
export function getCoffeeBySlug(_slug: string) { return undefined; }
