'use client';

// AdminStore is now a thin re-export barrel.
// All product/order state is fetched and managed locally inside each page view
// component using the API client in lib/api.ts. This file is kept for any
// shared types or helpers that multiple components may need.

export {};
