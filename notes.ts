// ========= NOTES ========== //
/**
 * 1) In the admin side: When editing a product, slug should be not editable to optional?
 * 2) Frontend-admin ( products ): Reason why the admin still authorized even after intentionally changing the token in the frontend
 *  - There's no http interceptor decorator in the product list controller
 *  - Think a way to validate the request e.g from main or admin
 * 3) Fix the admin dashboard overview - total products, lowstocks
 * 4) Add some clicked animation to every buttons.
 * 5) Create one reusable library to elimate redundant imports
 */
